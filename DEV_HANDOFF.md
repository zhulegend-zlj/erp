# ERP 开发交接摘要（工厂 → 家里，最新）

> 生成时间：2026-08-22 深夜（**本机 = 工厂电脑**，最新提交 `fc8490b`；本日全部工作=CSP_V3 按表格口径逐列重导 + 零件页布局/搜索 + 内嵌图片 106 张与 2D 图档 60 个零件入库 + 全量核对零差异，详见「当前阶段」与第 3 节；工厂电脑两库已部署全部迁移。**家里电脑**拉代码后先跑 `npx prisma migrate deploy` 再按第 6 节启动）
>
> **已完成的工作**：工程角色与权限分工（价格归采购）、大列表分页、流水联合查询、白屏修复、BOM 图片/新行置顶/列序、图档上传、零件排序、上传文件按 成品/零件 目录组织存放（文件名带零件名）、图片缩略图同比例显示、上传上下文修复、测试上传目录隔离、工程操作手册、备份脚本含 uploads。
>
> **加固专项（2026-08-22，本轮）**：全面走查 + 修复 + 补测试（后端 112/112 通过、前端 build 通过）。**行为变化，接手必知**：
> 1. 库存增减改为原子写（ON CONFLICT 条件更新），并发收货/领料/出货不再丢更新；
> 2. 订单状态禁止 PATCH 把 ready 直接置 shipped——只能通过「出货」页走 POST /api/shipments（前端订单页已同步隐藏该按钮）；状态迁移全部改为条件更新防并发重复；
> 3. 收货校验：零件必须属于该采购单、累计收货≤订购量、不良品≤收货量；收货后采购单状态自动更新 partial/received（此前永远 open）；
> 4. 手工建采购单校验零件供应商归属（与批量分组口径一致）；领料零件必须在该订单 BOM 内、成品入库成品必须在订单明细内，且仅 confirmed/in_production/ready 状态可操作（**如老板反馈过严可放宽**）；
> 5. 上传安全：移除 SVG、图片魔数校验、/uploads 需登录才能访问（nosniff）、超限 413、失败清理临时文件；工程账号不能写零件 supplierId（供应商归采购）；
> 6. 登录加失败限流（5 次锁 5 分钟）、金额/数量 zod 上界、全局 500 不再回显内部错误、分页参数严格解析、财务账期应收/应付扣已收/已付、前端 401 自动跳登录页。
>
> **工作流重构（2026-08-22 第二轮，按老板反馈）**：SalesOrder 新增 `purchasing`/`producing` 双阶段布尔列（迁移 `20260822050309_add_order_phase_flags`，**家里电脑拉代码后跑 `npx prisma migrate deploy`**）：
> 1. 状态分工：销售/老板只能 草稿↔已确认 与 已出货→已完成（PATCH）；老板额外可把 in_production/ready 强制回退到 confirmed（清空双阶段标志）；ready→shipped 仍只能走出货页；
> 2. 采购中：生成挂销售订单的采购单自动点亮，该订单全部采购单收货到 received 后自动熄灭；生产中：首次成品入库点亮，入库累计≥订单数量自动熄灭；两阶段都熄灭且状态 in_production 时自动推进 ready（待出货）；
> 3. 双阶段可同时存在（货没到齐也能先生产）；订单/看板/仓库的订单下拉统一用 `orderPhaseLabel`（common.ts）显示阶段标签，进入运作后不再显示「已确认」；
> 4. 销售单价权限：后端订单/出货单接口对 purchase/warehouse/engineer 剥离 unitPrice 与 otherCost（finance 保留成本利润汇总，sales/boss 完整）；
> 5. 采购页：keepAlive 会话级状态保持（已选订单/明细/弹窗草稿）；生成采购单弹窗带 SKU + 用量/需求/库存/需采购 + 供应商列（默认/未设置/本次改选标识），单价默认带出零件价格，批量接口支持行级 supplierId 覆盖，保存时可选择是否同步供应商回零件资料；
> 6. 仓库成品入库表单显示所选订单「已完成 X/Y 台」进度。
>
> **收货重构（2026-08-22 第三轮，按老板反馈）**：Receipt 表新增 supplierId 可空列、purchaseOrderId 改为可空（迁移 `20260822053726_receipt_self_buy`；**家里电脑拉代码后跑 `npx prisma migrate deploy`**）：
> 1. 收货两种模式：选采购单（自动带出零件/供应商只读，仓库只填本批实收数量，默认填未收数量、填 0 跳过；下方显示采购单明细，不含价格）或「自购买（无采购单）」（手工选零件+数量，供应商选填）；
> 2. QC 与不良品改为收货后补录：新增 GET /api/receipts（列表，分页/按采购单过滤）与 PATCH /api/receipts/:id（QC 状态/不良品数量≤收货量/来料单号），前端收货 Tab 下有「收货记录 QC 补录」面板；不良品仅记录不扣库存；
> 3. 修复采购页「仅本次生效」中止提交的 bug（现在仅本次生效也会继续生成采购单，只是不写回零件资料）；基础资料页 Tabs 当前页签 keepAlive。
> 4. 第四轮：已生成过采购单的订单可继续生成采购单（确认后新单关联同一销售订单，采购单列表新增「销售订单」列）；退补货表单网格化布局 + 下拉绑定采购单（物料限定该单零件/供应商自动带出/来料单号从该单收货记录选择；后端校验采购单存在且物料属于该单）。
> 5. 第五轮：领料出库绑定采购单（销售订单→采购订单两级选择，自动带出该单零件只填数量可删行，新增 /api/inventory/issue-context）；库存查询新增图片/SKU 列、名称/SKU 不区分大小写搜索；流水页三级级联查询（订单→采购单→零件，全不选查所有，新增 /api/inventory/ledger-search 服务端分页）。
> 6. 补漏：零件列表采购价格仅采购/老板可见（后端剥离 + 工程前端隐藏价格列 + 提交时剔除被隐藏字段），财务/仓库/销售同样看不到零件参考价。
>
> **当前阶段（2026-08-22 深夜，本日全部完成）**：
> 1. 零件页布局 = 表格口径：料号/图片/英文品名/中文名称/重量(g)/版本/材质/尺寸规格/表面处理/图档/价格/供应商（Description-EN、用在何处、生产工艺、序号、用量、单位、模具、起订量、图号 均不显示；模具/起订量/图号 按老板要求列表+表单一起去掉，序号/用量在 BOM 维护页按成品看）；
> 2. 零件页新增搜索框：料号/中文名称/英文品名不区分大小写模糊匹配（后端 /api/parts?search=，分页总数同步过滤）；
> 3. Part 新增 英文品名/重量/版本/材质/尺寸规格/表面处理/图号 字段（迁移 `20260822083931_part_sheet_columns`，**家里电脑拉代码后跑 `npx prisma migrate deploy`**），CSP_V3 按老板确认口径「清空零件与 BOM 后全新导入」（供应商按名复用不删）；
> 4. 数据全量核对零差异（`prisma/audit-csp-v3.ts`：107 行逐字段比对通过）；
> 5. 图片/图档已入库：Excel 内嵌图片 106 张归位（`import-csp-v3-images.ts`），2D PDF.rar 57 张图挂 60 个零件（`import-csp-v3-drawings.ts`，CSP-013 共用图×7、同编号多版本挂最新旧版留档）。
> 待办：①采购给零件补价格（表内 price 列全空）；②CSP-063 表内无此零件（图纸已跳过，如需补建零件再挂图）；③补图：CSP-027/CSP-058/CSP-060/49-002769/F LOGO/Lithium Grease 与大部分螺丝螺母暂无图档（找工程要）；④P1927-DAPM 等其他机型数据待工程提供后按同口径导入（import-csp-v3.ts 改路径/成品名复用）。
>
> **2026-08-25 同步（家里 → 工厂）**：家里 7 个提交已合并并部署到工厂电脑——领料多采购单+供应商标注+领料人记忆+库存显示、QC 默认合格、不良品/退补货实时联动（已退/已补/应补列）、收货/领料/成品入库/退补货「登记撤销+库存回滚」（void 冲销流水）、财务关联单改下拉、工程保存零件修复、FEEDBACK 测试隔离。工厂电脑恢复过程：新出现的 PostgreSQL 16 Windows 服务抢了 5432 端口导致后端与测试全挂——已停用该服务并设为手动启动（非有意安装，可卸载），便携版 PG 恢复 5432、数据完好。
> **审查修复（同日，已提交）**：撤销收货现会重算采购单状态（received/partial/open）并回退订单「采购中」（ready→in_production）；撤销成品入库现会重算「生产中」（未收满重新点亮、全部撤销保持 in_production 不待出货）；新增 refreshPurchasingPhaseAfterUndo / refreshProducingPhaseAfterUndo + 2 个回归测试（后端 135/135 通过）。HANDOFF.md 已改为指向本文件的说明（原内容过期，统一以本文件为准）。
>
> **2026-08-25 反馈轮（4 条，已处理）**：①零件页新增「成品」下拉筛选（默认全部零件，选中后只显示该成品 BOM 内零件，后端 /api/parts?productId=，可组合搜索与分页；按老板更正不显示用量列）；②③搜索框扩展为同时匹配 料号/中文名/英文品名/供应商/表面处理（EXISTS 关联 Supplier）；④BOM 维护页新增「导出表格」按钮（GET /api/products/:id/bom/export，列布局完全照 CSP_V3 清单 20 列含空列，文件名 erp-<成品SKU>-BOM-日期.xlsx，价格列仅采购/老板可见）。后端 137/137 测试通过。
>
> **2026-08-25 性能优化（老板同意）**：BOM 保存的文件归位逻辑批量化——受影响零件/零件→成品归属各 1 条 SQL；uploads 目录树只扫一遍建索引；位置未变且 URL 已就位时零操作（syncPartUrlsFromFolder 也改为 URL 未变不写库）。实测 CSP-V3 107 行 BOM 保存从 614ms 降到 ~50ms（12 倍）。后端 138/138 测试通过。
>
> **2026-08-25 BOM 导出升级（按老板反馈）**：导出改用 exceljs 生成——图片列嵌入零件缩略图（保持比例行高放大）、表头样式+冻结首行+全表细边框、每列自动筛选排序；新增依赖 `exceljs`（backend，xlsx 库仍用于导入）；导出 107 行含 106 图约 21.7MB、4s 左右。后端 138/138 测试通过。
>
> **2026-08-25 CSP_V3I 导入（老板复核后执行）**：新成品 CSP-V3I（名称「CSP V3I 脚踏板」老板自建）+ 80 新零件 + 146 行 BOM（67 个与 V3 共用）。口径：对照表驱动（prisma/import-csp-v3i.ts 读 D:\AI\erp-backups\CSP-V3I-SKU对照表.xlsx）；铝套管按尺寸命名 CSP-013-<长度>；CSP-005-深灰/CSP-033-灰色/包装泡棉 CSP-216-v3i/3M胶贴 CSP-321/扎线带 CSP-322/无纺布袋 CSP-323 与 V3 分列；图片 75 张、图档 42 新挂 + 3 更新（CSP-003/039/048，旧版 -图档2.pdf 留档）；CSP-017-v3i 待图纸。V3 料号口径变更（老板在 ERP 改）：铝套管 CSP-013-1..7 → CSP-013-<尺寸>、包装泡棉 CSP-216 → CSP-216-V3，audit-csp-v3.ts 已同步。遗留：CSP-003 文件夹被占用未归位到 _共用（关闭占用后重跑 import-csp-v3i.ts 即可，脚本幂等）。
>
> **2026-08-25 CSS_SQ 第三款导入**：成品 CSS-SQ（CSS_SQ 挂档器（黑色+USB））+ 74 新零件 + 82 BOM 行（8 共用）；官方料号照抄、新杂项 CSS-101 起编（老板确认）、磁铁/插销两行分开、CS_USB_A=CSS-115；图片 72、图档 36（CSS-016/CSS-058/CSS-HMC-V3 待补图纸）。脚本：prisma/preview-css-sq.ts（对照表）、prisma/import-css-sq.ts（导入）。库内零件总数 261。
>
> **2026-08-26 销售对接第一轮（本机=工厂电脑，订单删除上线）**：按老板拍板口径——①范围：无任何业务痕迹（无采购单/出货单/领料/成品入库/收款/库存流水）即可物理删除（草稿随时删；已确认未运作也可删）；②权限：销售+老板；③确认：前端弹窗须完整输入订单号。实现：后端 `DELETE /api/orders/:id`（事务内先查关联记录，有则 400 并逐项提示阻碍单据，绝不级联删除运作中数据）；前端订单列表加「删除」按钮（danger + 输入订单号确认，删空当前页自动回退一页）。新增 6 个回归测试，后端 **144/144** 通过、typecheck 干净、前端 build 通过、后端已重启。FEEDBACK「可以删除订单」已标 [已处理]。
>
> **2026-08-26 销售对接第二轮（本机=工厂电脑，出货单证中心上线）**：老板提供微信两份模板（收款发票 ZRH20260814006 Official Invoice、EU 商业发票+装箱单）并要求 ERP 出单。按老板拍板五项口径实现——①一次做全三份单证；②出货明细行式可拆行（同成品按柜/批次拆多行）；③箱数/毛净重/CBM 先手工填；④单证资料补进基础资料录一次自动带出（客户加 地址/VAT#/EORI/通知方，成品加 英文品名/海关编码，公司抬头+银行+VAT 做成「公司资料」配置老板可改）；⑤发票号手工填。**迁移 `20260826_shipment_documents`**（Customer/Product/Shipment 新列 + ShipmentLine 表 + CompanyProfile 表；**家里电脑拉代码后跑 `npx prisma migrate deploy`**）。新增 `src/utils/amount-words.ts`（英文大写金额）与 `src/domain/shipment-docs.ts`（三份模板还原生成器）。接口：POST /api/shipments 带明细行（数量合计与订单校验、按行扣库）、PATCH /api/shipments/:id 出货后补录单证、GET /api/shipments/:id/export?type=official|commercial|packing（仅销售/老板）、GET/PUT /api/company-profile（老板可改）。前端出货页升级为「单证中心」（明细行编辑+拆行、编辑单证弹窗、三份导出下拉），基础资料页新增客户/成品单证字段与「公司资料」页签。新增 9 个测试，后端 **153/153** 通过、typecheck 干净、前端 build 通过、后端已重启，三份单证与模板逐格对拍通过（大写金额/柜号/HBL/封条/ETD/ETA/合计均正确）。**公司资料还是空的，老板需在 基础资料→公司资料 补录抬头/银行/VAT（照模板填一次即可）**。
>
> **下一阶段（2026-08-26 起）：继续与销售对接**。待办：①老板在「公司资料」页补录抬头/银行/VAT 并在实际出货单上试用三份导出，把要调的地方反馈过来；②订单侧可按需再做 编辑交期/明细、客户 PO 号等；③另两条 [待处理] 中优反馈暂不动：成品图片地址改成图片（中）、流水/收发台账功能重合评估（中）。
>
> **在别处继续开发**：直接 `git pull origin main` 后按第 6 节启动；首次记得 `npx prisma migrate deploy` 与重建账号（见第 3 节）。
>
> **系统测试与修复（2026-08-23，本轮，dsh 全流程）**：自动化回归 126/126 通过（14 文件）+ 浏览器端到端走通完整业务闭环（工程录基础资料 → 采购建客户/供应商/价格 → 销售建单确认 → 采购生成采购单 → 仓库收货/QC/领料/成品入库 → 销售出货/运输节点 → 财务收付款 → 老板看板）。**本轮修复 4 个 bug**：
> 1. **未收货零件可直接领料产生负库存**（`domain/inventory.ts` applyStockChange INSERT 分支缺非负护栏，实测 TEST-103=-2）→ 改为无库存行且负变动时不插入（返回「库存不足」）；
> 2. **采购收齐后订单直接变「待出货」，跳过生产**（`domain/order-phase.ts` refreshPurchasingPhase 在无任何生产记录时推进 ready；旧 hardening 测试固化了该错误行为）→ 增加「已存在生产记录才允许推进 ready」+ 回归测试同步修正；
> 3. **工程无法新建/编辑零件**（`frontend/src/pages/Masters.tsx` 隐藏字段 price/supplierId 被转换循环以 null 写回 payload，后端 `'price' in body` 拒绝）→ 循环跳过 omitFields 键；
> 4. **财务收付款「关联单据」手输数字 ID 不友好**（`Finance.tsx`）→ 改为按单号可搜索下拉（历史未关联的演示收付款需补录关联后看板口径才准确）。
> 另修复：**测试污染 FEEDBACK.md**（feedback.test.ts 经真实路由向项目根追加「测试反馈-时间戳」条目）→ `FEEDBACK_PATH` 支持环境变量隔离，测试环境指向临时文件。
> **数据状态（本轮测试后）**：开发库仍为空库基础上创建了全套 TEST 演示数据——成品 TEST-100/200、零件 TEST-101~105/201/202/204、TEST测试客户/供应商、订单 SO-20260822-001/002、采购单 PO-20260822-001/002、收货/领料/成品入库/出货/收款 ¥300/付款 ¥42.6（其中收付款未关联单据），**均在数据库/上传目录，不在 git 内**；工厂电脑同步拉代码即可，数据按需保留或 `npx tsx --env-file=.env prisma/clear-all-data.ts` 清空。**遗留提醒**：①库内还有 5 个早期英文测试账号（boss/purchase/warehouse/sales/finance），上线前删除；②CSP_V3 精准数据尚未录入（空库 ± TEST 演示数据），老板反馈【待处理】"工程从零录入"仍待办。

## 1. 项目是什么

MTO（按单组装/贸易）ERP：销售订单 → BOM 需求 → 采购 → 收货 → 领料/成品入库 → 出货 → 财务 → 老板看板。

仓库实际业务参考了微信目录下的 8 个 Excel（成品出货、挂档器/杂项/脚板物料表、物料入出库、物料退补货）。

## 2. 项目位置与访问

- 代码目录：`D:\AI\erp`
- GitHub：https://github.com/zhulegend-zlj/erp.git（main）
- 后端：http://127.0.0.1:3000
- 前端：http://127.0.0.1:5173
- 局域网：http://192.168.1.114:5173（需防火墙放行 3000/5173）
- 数据库：PostgreSQL 16，127.0.0.1:5432，库 `erp` / `erp_test`
- PostgreSQL 便携版：`D:\AI\pg-dist\pgsql`，数据目录 `D:\AI\pg-data`
- 登录账号（中文）：老板 / 采购 / 仓库 / 销售 / 财务 / 工程，密码 88888888（工程=engineer 角色：负责录入成品/零件/BOM，不填价格；采购负责供应商维护、零件挂供应商、填价格）
- 工厂局域网访问需放行 3000/5173（脚本已入库：`scripts/allow-lan-firewall.ps1`，管理员 PowerShell 运行）

## 3. 当前数据状态

- **2026-08-22 晚：全部业务数据已清空，准备让工程开始精准录入**。开发库 `erp` 现仅保留 6 个中文账号（老板/采购/仓库/销售/财务/工程，密码 88888888），所有业务表（订单/采购单/收货/领料/入库/出货/财务/库存/流水/退补货/成品/零件/BOM/客户/供应商）与 `backend/uploads/` 已清空。清空前备份：`D:\AI\erp-backups\erp-before-clear-20260822143332.dump`（pg_dump）。
- 清库脚本：`cd backend && npx tsx --env-file=.env prisma/clear-all-data.ts`（保留用户账号，可复用）。
- **CSP_V3 已批量导入（2026-08-22，最终口径）**：成品 `CSP-V3`（CSP V3 挂档器）+ 107 个零件 + 107 条 BOM（脚本 `prisma/import-csp-v3.ts`，源表：微信文件目录 CSP_V3清单_物料明细.xlsx）。**命名规则**：①客户官方料号照抄（CSP-003 等）；②CSP-013 七长度变体 = CSP-013-1~7；③无料号自命名零件 = CSP-201 起顺序编号（CSP-2 开头=自命名段）；④螺丝/螺母/垫片等标准件 = 规格+类型（M3x13-杯头、M6-垫片），跨机种同规格共用同一料号，同 SKU 多行用量自动累加。**字段口径（2026-08-22 晚升级，逐列落库）**：英文品名/重量/版本/材质/尺寸规格/表面处理/图号 存独立字段（重量为文本，如 45.6/112g/线长确认）；用量进 BOM；供应商（Vendorid 列自动建供应商并挂接，'0'/'自己打印'/'改为自购' 跳过）；不录 Description-EN、用在何处、生产工艺（Part 表保留 usedIn/process 可选字段备用，界面不显示）；序号不进库（零件页不展示，BOM 页按成品看用量）。导入方式=先清空该成品 BOM 与其专属零件再全新导入，供应商按名复用，可重复执行（幂等）。每次导入生成对照表 `D:\AI\erp-backups\CSP-V3-SKU对照表.xlsx`（含供应商列）。**已全量核对（2026-08-22，`prisma/audit-csp-v3.ts`）**：107 行数据与库逐字段比对（SKU/中英文名/重量/版本/材质/尺寸/表面处理/用量/模具/起订量/图号/供应商/BOM）**零差异**；供应商 11 家挂 22 个零件正确，5 行非供应商值（改为自购×2、自己打印×2、0×1）按口径跳过。表格本身的口径/特殊值（非导入错误）：序号列缺 30、序号 67 出现两次（3M胶贴物理位置在第 53 行）；序号 78/81 两行各贴两张图（只取第一张）；CSP-203 扎线带表内无图；重量列个别值实为备注（线长确认/线长未确认）。待复核：序号9「铝套管20*77.8」用量空按 1。**图片已导入**：Excel「图片」列为内嵌图片（非文本），已用 `prisma/import-csp-v3-images.ts` 按锚点行号批量提取归位到 `uploads/CSP-V3/<零件SKU>-<零件名>/`（106/107 个零件有图；CSP-203 扎线带 表内无图；两行各有两张图只取第一张；表外 8 张游离图片跳过）。**图档（2D PDF）已导入**：`prisma/import-csp-v3-drawings.ts` 解包 `CSP_V3_2D PDF.rar`（57 个 PDF）——60 个零件已挂图档（CSP-013 一张图挂 7 个变体；CSP-011/CSP-071 挂最新版、旧版以 `-图档2.pdf` 留档），CSP-063 表内无此零件已跳过。仍无图档的：CSP-027 电路板主板 / CSP-058 磁铁 / CSP-060 PU泡棉 / 49-002769 产品安全手册 / F LOGO / Lithium Grease / 大部分标准螺丝螺母（rar 里只有 3 个 ISO 螺丝图）/ CSP-2xx 自命名杂项——如需要可找工程补图后同样脚本口径导入。
- 之前的粗略导入数据（成品 3 / 零件 557 / 供应商 65 / BOM 326）已删除，导出备份 Excel 仍在：`D:\AI\erp-backups\导入数据备份-20260822015812.xlsx`。**由「工程」账号从零录入精准数据**（先建成品 → 录零件/传图传档 → 配 BOM）。
- **待办数据**：P1927-DAPM 新机型的三张采购单（PO-DS-0217A/C/D，供应商晨鑫/铭亚/合丰磁铁）里的零件尚未录入——按分工由「工程」账号在基础资料录入零件（编号用采购单固有编号如 P1927-14872，不填价格），「采购」负责挂供应商、填价格。
- **数据恢复记录**：2026-08-22 开发时曾误用测试清库脚本清空过开发库，已通过 `npx tsx --env-file=.env prisma/import-real-data.ts` 从微信目录 Excel 全量重导（成品 3 / 零件 557 / 供应商 65 / BOM 326，记录 id 已重新生成）；业务数据（订单/采购单/库存/出货/财务）为空，需重新走流程测试。
- 图片/图档存储：`backend/uploads/`（已 gitignore），经 `/uploads/...` 访问。**目录组织策略（2026-08-22 起，文件名带名字）**：零件图片 `uploads/<成品SKU>/<零件SKU>-<零件名>/<零件SKU>-<零件名>.png`，图档 `.../<零件SKU>-<零件名>-图档.pdf`；挂多个成品的进 `_共用/`，未挂 BOM 的进 `_未分类/`（保存 BOM 自动归位并更新 URL）；成品图片 `uploads/<成品SKU>/<成品SKU>-<成品名>.png`。改名/删除零件、改成品 SKU/名称都会同步移动/重命名文件夹和文件并更新数据库路径。旧 uuid 文件已按老板指示全部删除，由工程重新上传精准数据。
- 导入脚本：`backend/prisma/import-real-data.ts`，其中 Excel 路径写死为本机微信目录。
- **家里数据库迁移**：家里库若缺 `engineer` 枚举值，跑 `cd backend && npx prisma migrate deploy`；6 个账号可用 `npx tsx --env-file=.env prisma/seed.ts` 重建（注意：会把所有账号密码重置回 88888888）。

## 4. 已完成功能

- 登录/改密，中文账号
- 首页分角色使用说明
- 基础资料：客户/供应商/成品/零件/BOM；零件列表按清单表格口径展示（料号/图片/英文品名/中文名称/重量/版本/材质/尺寸规格/表面处理/图档/价格/供应商，模具/起订量/图号不显示），图档支持上传 pdf/dwg/dxf/step/stp/igs/zip/xlsx 等（≤20MB）；新增「工程」角色分工——工程录成品/零件/BOM，采购只挂零件供应商
- 销售订单：新建、状态推进/回退、明细、删除（仅销售/老板；无业务痕迹才可删，有采购单/出货单/领料/入库/收款/流水则 400 提示阻碍单据，前端输入订单号二次确认）
- 出货（单证中心）：ready 校验、送货单号、签收人、备注、运输节点；出货明细行式（可拆行：客户PO/Lot/箱数/净重/毛重/CBM/柜号/封条/HBL）；整单单证字段（发票号/付款条件/贸易条款/唛头/原产地/海关编码/税率/船名航次/ETD/ETA/运费说明）；出货后 PATCH 补录；一键导出三份单证（收款发票 Official Invoice / 商业发票 Commercial Invoice / 装箱单 Packing List，照微信模板还原，含英文大写金额与收款记录）
- 采购：需求计算、按供应商自动分组生成多张采购单、销售单明细展示、采购单列表
- 仓库：收货（来料单号/QC/不良品）、领料、成品入库、退补货、库存、流水（物料 + 销售订单号联合查询，汇总需求/已出库/未出）、收发台账、订单物料计算
- 大列表分页：订单/库存/流水/收发台账/采购单/出货单/退补货/基础资料，后端 page/pageSize + 前端服务端分页
- 零件列表排序：按 SKU 字母前缀分组（同产品族）+ 组内数字升序，全站零件下拉统一
- 零件搜索：零件页表格上方搜索框，按料号/中文名称/英文品名不区分大小写模糊匹配（后端 /api/parts?search=，分页总数同步过滤，LIKE 特殊字符转义）
- 上传：图片（jpg/png/webp/gif/svg）+ 图档（pdf/dwg/dxf/step/stp/igs/zip/xlsx），单个 ≤20MB；按 成品/零件 目录自动归位，文件名带零件/成品名
- 图片缩略图同比例显示全貌（contain，非裁切），点击放大原图
- 财务：收付款、订单成本利润、账期
- 看板：应收/应付余额、订单进度
- 图片上传与静态服务

## 5. 反馈处理情况

> 反馈处理情况见 FEEDBACK.md 各条目的“处理”说明。2026-08-26 本会话处理：订单删除（高）。
>
> 未处理（截至 2026-08-26）：①成品图片地址改成图片（中，基础资料）；②流水/收发台账功能重合评估（中）。

## 6. 启动 / 重启方式

后端：
```powershell
cd D:\AI\erp\backend
npm run start
```

前端：
```powershell
cd D:\AI\erp\frontend
npm run dev -- --host 0.0.0.0
```

数据库启动（如未运行）：
```powershell
D:\AI\pg-dist\pgsql\bin\pg_ctl.exe -D D:\AI\pg-data -l D:\AI\pg-data\pg.log -o "-p 5432" start
```

## 7. 验证命令

```powershell
cd D:\AI\erp\backend
npm run typecheck
npm test

cd D:\AI\erp\frontend
npm run build
```

当前测试：153 个通过（2026-08-26 新增：订单删除 6 个回归 + 出货单证 9 个——英文大写金额 2、出货明细行/数量校验/PATCH/三份导出/公司资料权限 6、客户/成品单证字段 1）。

## 8. 注意事项

- 后端代码变更后需要重启后端；前端 Vite 通常自动热更新，但**偶发服务端转换缓存过期**（页面权限/UI 与磁盘代码不一致）——现象是“改了没生效”，处理：重启前端 dev 服务 + 浏览器 Ctrl+F5 硬刷新。
- ~~后端测试会向真实 `FEEDBACK.md` 追加“测试反馈”记录，跑完测试需要清理~~ → **已修复（2026-08-23）**：`FEEDBACK_PATH` 环境变量隔离，正常 `npm test` 不再污染 FEEDBACK.md。
- 数据库有 TEST 前缀演示数据时，看板/订单/采购等页面会看到测试单；正式录入前可用 `npx tsx --env-file=.env prisma/clear-all-data.ts` 清空（保留账号）。
- **测试清库防呆**：`src/test/helpers.ts` 的 `resetDb()` 已加保护——仅当 `DATABASE_URL` 含 `erp_test` 时才允许清库，误连开发库会直接抛错，不会再清空真实数据。
- 列表接口分页约定：传 `page`/`pageSize` 返回 `{ items, total, page, pageSize, totalPages }`；不传则返回全量数组（下拉框依赖）。非法分页参数返回 400。
- 上传接口 `/api/uploads`：按扩展名白名单校验（图片额外校验 MIME），最大 20MB（server.ts multipart limits）。
- 零件排序在 PostgreSQL 层（masters.ts 零件分支）：SKU 前缀分组 + 数字段 bigint[] 升序；修改时注意 regexp_matches 是集合函数，必须用 `array_agg` 子查询，否则行会被复制。
- Git 是便携版：`C:\Users\zhulianghong\Programs\Git\cmd\git.exe`，已加入用户 PATH；本机直连 GitHub 无需代理（家里的机器如失败，检查 `git config --global http.proxy`）。
- 上传文件目录 `backend/uploads/` 已 gitignore（根 .gitignore 同时忽略 `*.log`、`node_modules/`、`dist/`）。
- **测试上传目录隔离**：`setup-env.ts` 把 `UPLOAD_DIR` 指向系统临时目录（`uploads-store.ts` 读该环境变量），测试清理不会碰真实 uploads；不要在临时脚本里直接调用 `src/test/helpers.ts` 的 `resetDb()`（它连的是当前 DATABASE_URL，误连开发库会清空真实数据——虽有 erp_test 校验防呆，仍要小心）。
- **上传上下文**：前端上传时实时读取表单 SKU（未填先提示）；后端 `/api/uploads` 用 `req.parts()` 解析（`req.file()` 拿不到文本字段）；axios 实例不要设置全局 Content-Type（否则 multipart 会报「request is not multipart」）。
- `scripts/*.ps1` 已统一 UTF-8 BOM（PowerShell 5.1 中文乱码坑）。
- 工程操作手册：`docs/工程操作手册.html`（发工程）与 `.md`（仓库留档），里面已含表格批量导入格式约定。

