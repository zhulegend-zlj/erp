# MTO 工厂 ERP 专业级全面测试报告

- 测试日期：2026-08-27
- 测试对象：D:/AI/erp（Fastify5 + Prisma6 + PostgreSQL16 后端 + Vite/React18/AntD5 前端）
- 测试环境：后端 http://127.0.0.1:3000，前端 http://127.0.0.1:5173，主库 erp（真实数据只读）
- 测试账号：老板 / 采购 / 仓库 / 销售 / 财务 / 工程（密码 88888888）
- 测试方式：静态代码审查 + curl 带 cookie 动态接口测试（正/反用例、越权矩阵、并发、安全、健壮性、数据一致性）+ 前端 build/lint 走查 + 自动化回归

---

## 一、执行摘要

| 项目 | 结果 |
|---|---|
| 自动化回归（npm test） | ✅ 182/182 通过（18 个测试文件） |
| 后端 typecheck | ✅ 干净无错误 |
| 前端 build | ✅ 通过（14.63s，仅 chunk 体积提示） |
| 前端 lint | ✅ 0 错误、16 警告（react-hooks exhaustive-deps / set-state-in-effect 类，非阻断） |
| 静态审查 | 完成：backend/src/routes 13 个路由文件 + domain 5 个 + auth/errors/pagination + 前端 10 个页面/核心文件 |
| 动态接口测试 | 完成：100+ 条正/反断言，覆盖 6 角色越权矩阵、10 大功能域、并发、安全、健壮性、数据一致性 |
| 缺陷 | 共 12 项：**高 3 / 中 4 / 低 5** |
| 数据清理 | 已全部清理 TEST- 交易数据、测试客户/零件、上传文件、价格恢复，真实资料未动 |

### 缺陷统计

| 严重级 | 数量 | 编号 |
|---|---|---|
| 高 | 3 | BUG-01、BUG-02、BUG-03（并发超收/超产/超卖） |
| 中 | 4 | BUG-04、BUG-05、BUG-06、BUG-07 |
| 低 | 5 | BUG-08、BUG-09、BUG-10、BUG-11、BUG-12 |

> 结论：单请求主链路（登录→建单→采购→收货→领料→入库→排程→出货→财务→看板）功能正确、权限矩阵总体符合设计、分页/参数校验健壮。**主要风险集中在并发一致性（3 个高危）与出货后单据可编辑导致的数据一致性破坏**，建议上线前优先修复。

---

## 二、测试计划（按功能域）

### 1. 认证与权限
- [x] 6 角色中文账号登录、set-cookie、/auth/me 恢复登录态
- [x] 未登录访问业务接口/上传/静态资源 → 401
- [x] 越权矩阵：orders / schedules / dashboard / finance / parts / company-profile 逐角色 200/403 对照 requireRole
- [x] 写权限：订单(仅 sales)、采购单/收货(仅 purchase/warehouse)、出货/排程(仅 sales)、财务(仅 finance)、基础资料按资源分角色
- [x] 价格可见性：订单销售单价、零件采购价、采购单单价逐角色核对

### 2. 订单
- [x] 创建（客户PO号=订单号）、重复 PO 拦截 400、明细行交期必填、坏日期/负数量/1e12 拒绝
- [x] 编辑（草稿随意、已确认无业务痕迹可改、有业务锁定 400）
- [x] 状态推进/回退（销售仅 草稿↔已确认 与 已出货→已完成；老板强退）
- [x] 催确认（采购可催、销售确认后清空）、删除防呆（无痕迹可删、有痕迹 400）

### 3. 采购
- [x] 需求计算（BOM 爆炸 + 缺口 + 供应商/价格）、批量按供应商分组、Z001/Z002 编号递增
- [x] 手工采购单：零件归属供应商校验、明细去重、草稿订单拦截
- [x] 收货：归属校验、累计≤订购、状态自动 partial/received、撤销回滚

### 4. 库存与仓库
- [x] 收货入库/领料出库/成品入库/退补货
- [x] 领料零件必须在订单 BOM、未收货负库存拦截、成品入库≤订单数量
- [x] 库存列表/流水/台账/订单物料计算/领料上下文

### 5. 出货与排程
- [x] 排程创建（不超过订单剩余）、仓库标记已备好、销售改/取消/删除
- [x] 出货两模式（排程拼票 / 无排程手工）、部分出货、出满自动 shipped、排程扣减
- [x] 出货后补录单证、运输节点、三份单证导出

### 6. 财务
- [x] 收/付款、关联单据归属校验、金额/日期校验、账期清单、成本利润汇总

### 7. 基础资料
- [x] 客户/供应商/成品/零件/BOM CRUD 与角色分工、零件搜索/排序/成品筛选、BOM 导出

### 8. 看板
- [x] 老板/采购摘要、应收/应付余额口径、待采购提醒、越权 403

### 9. 上传与单证
- [x] 图片魔数校验、扩展名白名单、上传角色限制、/uploads 登录+nosniff+no-cache
- [x] parse-image 一键识别（无文件/非图片边界）

### 10. 安全与健壮性
- [x] 存储型 XSS、SQL 注入特殊字符、IDOR/坏 ID、分页边界、超长串/emoji、超界数字、坏日期、并发

---

## 三、静态审查发现

### 3.1 权限漏洞 / 越权
1. **销售可把排程直接标记「已备好」**（schedules.ts PATCH 对 sales/boss 允许 status∈{pending,picked,cancelled}），绕过仓库备货环节。→ BUG-08
2. **手工出货旁路**：POST /api/shipments 仍接受 salesOrderId + lines 手工模式，仅拦截 draft/shipped/completed，不校验 ready，可从 confirmed/in_production 直接出货。→ BUG-06
3. **采购单价无剥离**：GET /api/purchase-orders 对 5 角色一律返回明细 unitPrice，与「采购价格仅采购/老板可见」口径不一致。→ BUG-09

### 3.2 zod 校验缺失 / 边界
4. 领料 issueItemSchema.qty、成品入库 productionEntrySchema.qty、退补货 returnQty/replenishQty 均**无上界**（其余金额/数量均有 max）。
5. 客户/供应商/零件/成品 name、feedback content、退补货 note/lotNo 等**无长度上限**（实测 10000 字符被接受）。→ BUG-11
6. 排程 PATCH 改数量时**未复用创建时的「订单剩余」校验**。→ BUG-05
7. 出货后 PATCH 明细行允许改 qty/productId，不重算库存、不校验订单。→ BUG-04

### 3.3 事务 / 并发
8. 收货累计校验（receipt.groupBy 求和后比较）、成品入库累计校验（productionEntry.aggregate）、出货已出校验（shipmentLine.groupBy）均为**读-判-写，无数据库层约束/锁**，并发下可超收/超产/超卖。→ BUG-01/02/03

### 3.4 错误处理 / 信息泄漏
9. 多个路由 catch 用 err.message 做 include 子串匹配并原样回显（inventory/purchasing/shipping/returnReplenish），Prisma 校验/连接器错误会**把内部文件路径、源码片段、ConnectorError 细节返回给客户端**，且源码片段中若恰好包含「不能领料」等关键词会被误判为业务错误返回 400。→ BUG-07
10. Fastify FST_ERR_CTP_INVALID_CONTENT_LENGTH（请求体 Content-Length 不匹配）未映射，落 500。→ BUG-10
11. 不存在的路由（如 GET /api/receipts/:id）返回 Fastify 默认 404，回显内部路由路径。

### 3.5 其他
12. 订单详情对 finance 显示销售单价，但出货单列表对 finance 隐藏单价（口径不一致，低风险）。
13. FEEDBACK.md 历史口径「销售订单由销售/老板在订单页创建」与现实现「仅 sales 可创建（前端对老板隐藏新建按钮）」不一致（代码内部一致，属文档滞后）。

---

## 四、动态测试用例与结果

### 4.1 认证与权限（越权矩阵）

| # | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| A1 | 6 角色中文账号登录 | 200 | 200 | ✅ |
| A2 | 未登录访问 /api/orders | 401 | 401 | ✅ |
| A3 | 未登录访问 /uploads/... | 401 | 401 | ✅ |
| A4 | engineer 访问 orders/schedules/dashboard/finance/due | 403 | 403 | ✅ |
| A5 | boss 创建订单 POST /api/orders | 403(仅 sales) | 403 | ✅ |
| A6 | sales 生成采购单/收货/领料 | 403 | 403 | ✅ |
| A7 | warehouse PATCH 订单状态 | 403 | 403 | ✅ |
| A8 | warehouse PATCH 排程 status=picked | 200 | 200 | ✅ |
| A9 | warehouse PATCH 排程带额外字段 | 400 | 400 | ✅ |
| A10 | sales PATCH 排程 status=picked | 400 | 200 | ❌ BUG-08 |
| A11 | sales 创建供应商 / warehouse 创建客户 | 403 | 403 | ✅ |
| A12 | sales 创建客户 | 200 | 200 | ✅ |
| A13 | finance 订单成本汇总 sales 访问 | 403 | 403 | ✅ |
| A14 | 订单单价：sales/finance/boss 可见，purchase/warehouse 隐藏 | 隐藏 | 符合 | ✅ |
| A15 | 零件价格：purchase/boss 可见，其余隐藏 | 隐藏 | 符合 | ✅ |
| A16 | 采购单单价对 sales/warehouse 隐藏 | 应隐藏 | 可见 | ❌ BUG-09 |

### 4.2 订单域

| # | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| B1 | 创建订单（明细含两个交期） | 200 | 200 | ✅ |
| B2 | 重复客户PO号 | 400 | 400 | ✅ |
| B3 | 明细交期非法/负数/1e12 | 400 | 400 | ✅ |
| B4 | 编辑草稿/已确认无业务 | 200 | 200 | ✅ |
| B5 | 状态推进 draft→confirmed、confirmed→draft | 200 | 200 | ✅ |
| B6 | sales 推进 confirmed→in_production / →shipped | 400 | 400 | ✅ |
| B7 | 采购催确认(草稿 200 / sales 403) | 200/403 | 符合 | ✅ |
| B8 | 删除无痕迹订单 | 200 | 200 | ✅ |
| B9 | 删除有采购单的订单 | 400 | 400（提示 1 张采购单） | ✅ |

### 4.3 采购域

| # | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| C1 | 批量生成采购单（按供应商分组） | 200 | 200（2 张，Z001/Z002） | ✅ |
| C2 | 采购单号 = 订单号-Z00N 递增 | 递增 | 符合 | ✅ |
| C3 | 手工采购单零件供应商不匹配 | 400 | 400 | ✅ |
| C4 | 需求计算（BOM 缺口/供应商/价格） | 200 | 200 | ✅ |
| C5 | 收货基本/超订购/非本单零件 | 200/400/400 | 符合 | ✅ |
| C6 | sales 收货 | 403 | 403 | ✅ |
| C7 | 8 并发收货（订购 2，各收 1） | 仅 2 成功 | 6 成功 | ❌ BUG-01 |

### 4.4 库存与仓库

| # | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| D1 | 未收货零件领料 | 400 库存不足 | 400 | ✅ |
| D2 | 领料零件不在订单 BOM | 400 | 400 | ✅ |
| D3 | 成品入库≤订单数量 / 超限 | 200/400 | 符合 | ✅ |
| D4 | 5 并发成品入库（订单 12 台，各入 12） | 仅 1 成功 | 2 成功（入 24） | ❌ BUG-02 |

### 4.5 出货与排程

| # | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| E1 | 创建排程 / 超订单剩余 | 200/400 | 符合 | ✅ |
| E2 | PATCH 排程数量 3→100（订单 5） | 400 | 200 | ❌ BUG-05 |
| E3 | 排程部分出货 → 剩余保留 → 出满自动 shipped | 200 | 符合 | ✅ |
| E4 | 手工模式从 confirmed 订单出货（无排程无生产） | 应拒绝 | 200 | ❌ BUG-06 |
| E5 | 出货后 PATCH 明细数量 12→100 | 应拒绝 | 200，shippedQty 虚高、库存未动 | ❌ BUG-04 |
| E6 | 3 并发出货（订单 12 台，各出 12） | 仅 1 成功 | 3 成功（出 36） | ❌ BUG-03 |

### 4.6 财务

| # | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| F1 | 供应商付款 / 客户收款 | 200 | 200 | ✅ |
| F2 | 付款关联不属于该供应商的采购单 | 400 | 400 | ✅ |
| F3 | 收款关联不属于该客户的订单 | 400 | 400 | ✅ |
| F4 | 负金额 / 坏日期 | 400 | 400 | ✅ |
| F5 | 账期清单（finance/boss 200，其余 403） | 200/403 | 符合 | ✅ |

### 4.7 基础资料 / 看板

| # | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| G1 | 工程建零件带价格 | 400 | 400 | ✅ |
| G2 | 工程建零件不带价格 | 200 | 200 | ✅ |
| G3 | 采购改零件仅限 supplierId/price，改名称 | 400 | 400 | ✅ |
| G4 | 零件搜索特殊字符（' OR '1'='1、%） | 200 空结果不报错 | 200 | ✅ |
| G5 | 看板 summary（boss/purchase 200，其余 403） | 200/403 | 符合 | ✅ |

### 4.8 上传与单证

| # | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| H1 | 假 png（文本改扩展名）上传 | 400 魔数不符 | 400 | ✅ |
| H2 | 真 png 上传 | 200 | 200 | ✅ |
| H3 | sales 上传 / 未登录上传 | 403/401 | 403/401 | ✅ |
| H4 | /uploads 响应头 nosniff + no-cache | 有 | 有 | ✅ |
| H5 | parse-image 无文件 | 400 | 500 | ❌ BUG-12 |

### 4.9 安全与健壮性

| # | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| I1 | 客户名存 script 标签 | 应转义/过滤 | 原样存储并返回（前端 React 转义） | ⚠️ 见风险 |
| I2 | 零件/库存搜索 SQL 注入字符 | 不报错不泄漏 | 200 空结果 | ✅ |
| I3 | IDOR：orders/999999、orders/abc、receipts/999999 | 404/400 | 404/400（receipts 泄漏路由路径） | ✅/⚠️ |
| I4 | page=-1 / abc / 999999；pageSize=0 / 201 | 400 | 400 | ✅ |
| I5 | emoji + 10000 字符客户名 | 应限制长度 | 200 接受 | ❌ BUG-11 |
| I6 | 订单 qty 1e12 | 400 | 400 | ✅ |
| I7 | 领料 qty 1e12 | 400 中文提示 | 400 但泄漏内部路径/源码 | ❌ BUG-07 |
| I8 | 请求体 Content-Length 不匹配 | 400 | 500 | ❌ BUG-10 |

### 4.10 数据一致性（抽查）

- 收货/领料/入库/出货后 Stock 与 InventoryLedger 自洽（单请求场景 ✅）。
- 并发场景下 Stock 与单据脱节（超收/超产/超卖，❌ 见 BUG-01/02/03）。
- 出货后编辑单证导致 shippedQty(124) 远超 totalQty(17) 而库存不变（❌ BUG-04）。

---

## 五、缺陷列表

### BUG-01【高】收货并发可超收订购量
- 接口：POST /api/receipts
- 现象：累计收货校验（receipt.groupBy 求和 → 比较订购量）在事务内读-判-写，无数据库层约束。
- 复现：新建采购单（订购 2 台），用 8 个并发 curl 各收 1 台 → **6 个返回 200**，库存 +6、收货记录 6 条（订购 2）。
- 证据：8x conc codes: 200 200 200 200 200 200 400 400；stock3363=6 / receiptCount=6。
- 建议：加数据库层累计约束（如收货量采用 INSERT ... WHERE (SELECT 已收合计) < 订购量 或对该采购单行加 SELECT FOR UPDATE 锁），或唯一约束+原子递增。

### BUG-02【高】成品入库并发可超订单数量
- 接口：POST /api/production-entries
- 现象：already = aggregate(_sum) 后 create，无锁。
- 复现：订单 12 台，5 个并发各入库 12 台 → **2 个返回 200**，入库 24 台、成品库存 +24。
- 证据：5x prod conc codes: 200 200 400 400 400；prodCount=2 / sum=24。
- 建议：与 BUG-01 相同——入库累计改为条件写入或对订单行加锁。

### BUG-03【高】并发出货可超卖
- 接口：POST /api/shipments
- 现象：手工/排程模式「已出+本批 ≤ 订单数量」用 shipmentLine.groupBy 读后判，并发下竞态；库存检查仅在库存=订单数时兜底，当该成品库存被其他订单共享/超产时即被绕过。
- 复现：订单 12 台（成品库存 124），3 个并发各出 12 台 → **3 个全部 200**，出 36 台，订单误判「已出货」。
- 证据：3x ship conc codes: 200 200 200；shipLines=3 / sum=36；订单 status=shipped。
- 建议：出货校验改为原子条件（如 INSERT ... SELECT ... WHERE 订单已出合计 + 本批 ≤ 订单数量）或对订单行加 SELECT FOR UPDATE。

### BUG-04【中】出货后编辑单证可改数量/成品，不重算库存、不校验订单
- 接口：PATCH /api/shipments/:id
- 现象：lines 整体替换允许改 qty/productId，删除重插明细行，但**不扣/回补库存、不校验订单数量**；订单「已出」按明细行实时汇总，导致已出数虚高、库存与单据脱节。
- 复现：出货单明细 qty 12→100，PATCH 返回 200；订单 shippedQty=124、totalQty=17，成品库存不变。
- 建议：单证补录仅允许改单证字段（箱数/毛净重/CBM/柜号/发票号等），禁止改 productId/qty；确需改明细须走撤销/冲销流程。

### BUG-05【中】排程编辑数量不校验订单剩余，可据此超量出货
- 接口：PATCH /api/schedules/:id
- 现象：改 qty 时未复用创建时的「排程+已出 ≤ 订单数量」校验。
- 复现：订单 5 台，排程 3→PATCH 100 返回 200；随后从该排程出货 100 台成功，订单 shippedQty=100 > totalQty=5。
- 建议：PATCH qty 时按订单剩余重新校验（与 POST /api/schedules 同口径）。

### BUG-06【中】手工出货旁路绕过「待出货/排程」约束
- 接口：POST /api/shipments（salesOrderId + lines 模式）
- 现象：仅拦截 draft/shipped/completed，不要求 ready，也不要求排程；可从 confirmed/in_production 直接出货，产生「订单未 ready 却已有出货量」的状态不一致。
- 复现：confirmed 订单（无排程无生产）POST /api/shipments {salesOrderId, lines:[{qty:1}]} 返回 200，订单仍 confirmed 但 shippedQty=1/2。
- 建议：下线/禁用手工模式，统一走排程；或补齐 ready 校验。

### BUG-07【中】领料超界整数触发 Prisma 错误，泄漏内部路径与源码
- 接口：POST /api/issues（qty=1e12）
- 现象：issueItemSchema.qty 无上界，Prisma 校验错误 Unable to fit integer value into INT4 未映射，route catch 用 message.includes('不能领料') 误匹配到错误信息里携带的源码片段，返回 400 且原样回显内部信息。
- 证据：响应含 Invalid tx.issue.create() invocation in D:/AI/erp/backend/src/routes/inventory.ts:76:40 ... ConnectorError ... Unable to fit integer value '1000000000000' into an INT4。
- 建议：①issue/production/return-replenish 数量加 .max(2147483647)；②统一错误映射——未识别的 Prisma 错误走全局 500 且不回显 err.message，移除 catch 里对 err.message 的子串匹配或改为对受控业务异常的 code 匹配。

### BUG-08【低】销售可把排程直接标记「已备好」，绕过仓库备货
- 接口：PATCH /api/schedules/:id（sales/boss 可 status=picked）
- 证据：sales {"status":"picked"} → 200，随后可直接出货；仓库二次标记返回 400。
- 建议：picked 仅限 warehouse（sales/boss 可改数量/日期/取消，不可 picked）。

### BUG-09【低】采购单价对销售/仓库可见，与「采购价格仅采购/老板可见」口径不一致
- 接口：GET /api/purchase-orders
- 证据：sales/warehouse/finance 均拿到明细 unitPrice（零件价格字段对这几类角色已隐藏，采购单明细却未隐藏）。
- 建议：按同一口径对非 采购/老板/财务 角色剥离采购单价（财务需成本可见，销售/仓库应隐藏）。

### BUG-10【低】请求体 Content-Length 不匹配返回 500 而非 400
- 现象：Fastify FST_ERR_CTP_INVALID_CONTENT_LENGTH（statusCode 400）未在全局错误处理器映射，落 500。
- 建议：在 setErrorHandler 中按 error.statusCode（4xx）透传，仅 5xx 才统一「服务器错误」。

### BUG-11【低】客户/供应商/零件名称等无长度上限
- 证据：客户 name 10000 字符 + emoji → 200 接受入库。
- 建议：对 name/备注/反馈内容等文本字段加 max()（如 200）上限。

### BUG-12【低】一键识别图片无文件/非 multipart 返回 500
- 接口：POST /api/orders/parse-image
- 证据：无请求体 POST → 500「服务器错误」（req.parts() 抛错未捕获），预期 400。
- 建议：捕获 multipart 解析错误并返回 400「未收到图片文件」。

---

## 六、风险与建议

1. **并发一致性是上线前最高优先级**：BUG-01/02/03 属同一类「读-判-写」竞态，建议统一引入数据库层原子条件写入或 SELECT FOR UPDATE，并补并发回归测试。
2. **单据可编辑范围过大**（BUG-04/05/06）：出货/排程生成后应锁定数量与成品，只允许补录单证文本，避免「账实不符」。
3. **错误信息泄漏面偏大**：建议全局统一错误契约，所有未识别异常只返回「服务器错误」，内部详情只进日志；受控业务校验用错误码而非 message.includes。
4. **输入边界补齐**：数量/文本长度统一设上界，与既有金额/订单数量的 max 口径一致。
5. **价格口径统一**：订单单价、零件价格、采购单单价三处可见性口径应统一成一张权限矩阵，避免遗漏。
6. 文档口径（FEEDBACK「老板可建单」）与代码（仅 sales 可建单）不一致，建议同步文档或放开权限。

---

## 七、测试数据清理说明

- 本次动态测试全部使用 TEST- 前缀数据（客户 TEST测试客户 id=8、成品 TEST-P100/TEST-P200、零件 TEST-101~110 / TEST-201~210、供应商 TEST供应商A/B）。
- 已清理：新建的 TEST-QA 订单（含明细）、采购单（含明细）、收货、成品入库、出货单（含明细与排程回写）、出货排程、收/付款、库存流水、测试客户（含 XSS/emoji 样本）、测试零件、测试上传文件。
- 已恢复：TEST-101 零件价格恢复为 98.66；TEST 零件/成品库存恢复到测试前快照（零件 0、TEST-P100=100、TEST-P200=200）。
- 未动：可赛尔客户、CSP_V3/CSP_V3I/CSS_SQ 成品、零件、BOM、公司资料、到货仓、既有订单 265440/265441 及其排程/出货。
- 无遗留 TEST 数据；服务进程（3000/5173/PG）全程保持运行。

---

## 八、缺陷修复记录（2026-08-27 同日，老板拍板「修」）

12 项缺陷已全部修复，后端测试 182 → **188/188**（新增 6 个回归：并发收货/并发入库/并发出货/排程已备好权限/图片识别 400/采购价隐藏），typecheck 干净、前端 build 通过、后端已重启。

| 编号 | 修复方式 |
|---|---|
| BUG-01 | 收货事务先 SELECT FOR UPDATE 锁采购单行，并发串行化 |
| BUG-02 | 成品入库事务先锁订单行，同上 |
| BUG-03 | 出货事务先锁排程行+订单行（按 id 排序防死锁），并加「每订单每成品 已出+本批≤订单数量」双保险 |
| BUG-04 | PATCH /api/shipments/:id 禁止增删行、改成品/数量/单价（400 提示），仅可补录单证文字 |
| BUG-05 | PATCH /api/schedules/:id 改数量复用「订单剩余」校验（其他排程+已出+新数量≤订单数量） |
| BUG-06 | 手工出货模式后端下线：POST /api/shipments 无 schedules 一律 400「已停用，请走排程」 |
| BUG-07 | 数量字段全部加 max(2147483647)；新增 errors.routeError 统一出口——业务中文错误 400/404、其余一律 500 通用文案不回显内部细节，替换各路由 catch 的 message.includes 逻辑 |
| BUG-08 | 排程「已备好」仅 warehouse 可标；sales/boss 传 picked 返回 400「只有仓库可以标记」 |
| BUG-09 | GET /api/purchase-orders 对 sales/warehouse/engineer 剥离明细 unitPrice（采购/老板/财务可见） |
| BUG-10 | 全局错误处理器透传 Fastify 4xx（Content-Length 不匹配等）→ 400「请求格式错误」 |
| BUG-11 | 客户/供应商/成品/零件名称 max 200、SKU max 60、反馈内容 max 2000、退补货备注 max 500/来料单号 max 100 |
| BUG-12 | parse-image multipart 解析失败统一 400「未收到图片文件」 |

*报告完*
